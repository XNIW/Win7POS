<chrome:DialogShellWindow x:Class="Win7POS.Wpf.Pos.Dialogs.RoleEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:Win7POS.Wpf.Chrome"
        Title="Modifica ruolo"
        ShowHeader="False"
        UseModalOverlay="True"
        DialogCornerRadius="16"
        DialogBackground="#F1EDF7"
        DialogBorderBrush="#D9CEE8"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        MinWidth="320"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="24" Background="#F1EDF7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Modifica ruolo" FontSize="18" FontWeight="SemiBold" Foreground="#5E3F86" Margin="0,0,0,16"/>
        <Border Grid.Row="1" Background="#FCFBFE" BorderBrush="#E4DBEF" BorderThickness="1" CornerRadius="10" Padding="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*" MinWidth="180"/>
            </Grid.ColumnDefinitions>
            <Grid x:Name="CodeRow" Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*" MinWidth="180"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="CodeLabel" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,12,0" Text="Codice ruolo:"/>
                <StackPanel Grid.Column="1">
                    <TextBox x:Name="CodeBox" Margin="0,0,0,4" Style="{StaticResource ModernTextBoxStyle}" MinHeight="34"/>
                    <TextBlock Text="Interno, univoco, minuscolo, senza spazi. Es: dipendente" Foreground="#8873A4" FontSize="11" Margin="0,0,0,10"/>
                </StackPanel>
            </Grid>
            <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,12,0" Text="Nome visibile:"/>
            <StackPanel Grid.Row="1" Grid.Column="1">
                <TextBox x:Name="NameBox" Margin="0,0,0,4" Style="{StaticResource ModernTextBoxStyle}" MinHeight="34"/>
                <TextBlock Text="Testo mostrato all'operatore. Es: Dipendente" Foreground="#8873A4" FontSize="11" Margin="0,0,0,10"/>
            </StackPanel>
            <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="OK" MinWidth="90" Height="36" Margin="0,0,8,0" Click="OnOkClick" Style="{StaticResource PrimaryButtonStyle}" IsDefault="True"/>
                <Button Content="Annulla" MinWidth="90" Height="36" IsCancel="True" Style="{StaticResource OutlineButtonStyle}" Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
        </Border>
    </Grid>
</chrome:DialogShellWindow>
