<chrome:DialogShellWindow x:Class="Win7POS.Wpf.Pos.Dialogs.ExitConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:Win7POS.Wpf.Chrome"
        xmlns:loc="clr-namespace:Win7POS.Wpf.Localization"
        Title="{loc:Loc exit.title}"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        UseModalOverlay="True">
    <Grid MinWidth="440" MaxWidth="560">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Text="{loc:Loc exit.title}" Style="{StaticResource DialogTitleStyle}"/>
            <TextBlock Text="{loc:Loc exit.question}" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{StaticResource TextPrimaryBrush}"/>
        </StackPanel>

        <Border Grid.Row="1" x:Name="CartWarning" Margin="0,16,0,0" Padding="14" CornerRadius="8" Background="#FFF4D6" BorderBrush="#D6A93D" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="{loc:Loc exit.cartWarning}" FontWeight="SemiBold" Foreground="#6B4300" TextWrapping="Wrap"/>
                <TextBlock x:Name="CartSummaryText" Margin="0,6,0,0" Foreground="#6B4300"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="{StaticResource DialogFooterMargin}">
            <Button Style="{StaticResource DialogCancelButtonStyle}" Click="OnMinimizeClick" Margin="0,0,8,0">
                <TextBlock Text="{loc:Loc exit.minimize}"/>
            </Button>
            <Button Style="{StaticResource DialogActionButtonStyle}" Click="OnCloseApplicationClick" Margin="0,0,8,0">
                <TextBlock Text="{loc:Loc exit.closeApplication}"/>
            </Button>
            <Button Style="{StaticResource DialogCancelButtonStyle}" IsCancel="True" Click="OnCancelClick">
                <TextBlock Text="{loc:Loc common.cancel}"/>
            </Button>
        </StackPanel>
    </Grid>
</chrome:DialogShellWindow>
