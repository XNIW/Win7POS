<chrome:DialogShellWindow x:Class="Win7POS.Wpf.Pos.Dialogs.DbMaintenanceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:Win7POS.Wpf.Chrome"
        xmlns:loc="clr-namespace:Win7POS.Wpf.Localization"
        Title="{Binding Source={x:Static loc:PosLocalization.Current}, Path=[dbMaintenance.title]}"
        ShowHeader="False"
        DialogCornerRadius="16"
        DialogBackground="#F1EDF7"
        DialogBorderBrush="#D9CEE8"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Manual"
        Width="680"
        Height="520"
        MinWidth="640"
        MinHeight="460"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    <Grid Margin="0" Background="#F1EDF7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{loc:Loc dbMaintenance.title}" Style="{StaticResource DialogTitleStyle}" Margin="0,0,0,10"/>
        <Border Grid.Row="1" Background="#FCFBFE" BorderBrush="#E4DBEF" BorderThickness="1" CornerRadius="10" Padding="18">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="170"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="{loc:Loc dbMaintenance.dbPath}"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="8,0,0,0" IsReadOnly="True" Text="{Binding DbPath, Mode=OneWay}" Style="{StaticResource ModernTextBoxStyle}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,6,0,0" VerticalAlignment="Center" Text="{loc:Loc dbMaintenance.backupsDirectory}"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="8,6,0,0" IsReadOnly="True" Text="{Binding BackupsDirectory, Mode=OneWay}" Style="{StaticResource ModernTextBoxStyle}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,6,0,0" VerticalAlignment="Center" Text="{loc:Loc dbMaintenance.exportsDirectory}"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="8,6,0,0" IsReadOnly="True" Text="{Binding ExportsDirectory, Mode=OneWay}" Style="{StaticResource ModernTextBoxStyle}"/>

                    <WrapPanel Grid.Row="3" Grid.ColumnSpan="2" Margin="0,12,0,0">
                        <Button x:Name="BackupNowButton" MinWidth="120" Height="36" Margin="0,0,8,8" Content="{loc:Loc dbMaintenance.backupNow}" Command="{Binding BackupNowCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                        <Button x:Name="RestoreBackupButton" MinWidth="140" Height="36" Margin="0,0,8,8" Content="{loc:Loc dbMaintenance.restoreBackup}" Command="{Binding RestoreBackupCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                        <Button MinWidth="130" Height="36" Margin="0,0,8,8" Content="{loc:Loc dbMaintenance.integrityCheck}" Command="{Binding IntegrityCheckCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                        <Button MinWidth="180" Height="36" Margin="0,0,8,8" Content="{loc:Loc dbMaintenance.completeRestoreReview}" Command="{Binding CompleteRestoreReviewCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                        <Button x:Name="VacuumButton" MinWidth="110" Height="36" Margin="0,0,8,8" Content="VACUUM" Command="{Binding VacuumCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                        <Button x:Name="SupplierImportButton" MinWidth="170" Height="36" Margin="0,0,8,8" Content="{loc:Loc supplierExcelImport.title}" Command="{Binding SupplierExcelImportCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                        <Button x:Name="OpenFolderButton" MinWidth="120" Height="36" Margin="0,0,8,8" Content="{loc:Loc dbMaintenance.openFolder}" Command="{Binding OpenFolderCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                    </WrapPanel>

                    <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Margin="0,4,0,0" Foreground="DimGray" Text="{loc:Loc dbMaintenance.outputResults}"/>
                    <TextBox Grid.Row="5" Grid.ColumnSpan="2" Margin="0,6,0,0" MinHeight="120"
                             Text="{Binding OutputLog}"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             Style="{StaticResource ModernTextBoxStyle}"/>
                </Grid>
            </ScrollViewer>
        </Border>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="{StaticResource DialogFooterMargin}">
            <Button MinWidth="110" Height="42" Content="{loc:Loc common.close}" IsCancel="True" Style="{StaticResource DialogCancelButtonStyle}" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</chrome:DialogShellWindow>
