<chrome:DialogShellWindow x:Class="Win7POS.Wpf.Pos.Dialogs.DbMaintenanceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:Win7POS.Wpf.Chrome"
        Title="Manutenzione database"
        ShowHeader="False"
        DialogCornerRadius="16"
        DialogBackground="#F1EDF7"
        DialogBorderBrush="#D9CEE8"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Manual"
        Width="680"
        Height="520"
        MinWidth="640"
        MinHeight="460"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    <Grid Margin="24" Background="#F1EDF7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Manutenzione database" FontSize="18" FontWeight="SemiBold" Foreground="#5E3F86" Margin="0,0,0,16"/>
        <Border Grid.Row="1" Background="#FCFBFE" BorderBrush="#E4DBEF" BorderThickness="1" CornerRadius="10" Padding="18">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="170"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="DB Path:"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="8,0,0,0" IsReadOnly="True" Text="{Binding DbPath}" Style="{StaticResource ModernTextBoxStyle}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,6,0,0" VerticalAlignment="Center" Text="Backups Directory:"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="8,6,0,0" IsReadOnly="True" Text="{Binding BackupsDirectory}" Style="{StaticResource ModernTextBoxStyle}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,6,0,0" VerticalAlignment="Center" Text="Exports Directory:"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="8,6,0,0" IsReadOnly="True" Text="{Binding ExportsDirectory}" Style="{StaticResource ModernTextBoxStyle}"/>

                    <WrapPanel Grid.Row="3" Grid.ColumnSpan="2" Margin="0,12,0,0">
                        <Button MinWidth="120" Height="36" Margin="0,0,8,8" Content="Backup Now" Command="{Binding BackupNowCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                        <Button MinWidth="140" Height="36" Margin="0,0,8,8" Content="Restore Backup..." Command="{Binding RestoreBackupCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                        <Button MinWidth="130" Height="36" Margin="0,0,8,8" Content="Integrity Check" Command="{Binding IntegrityCheckCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                        <Button MinWidth="110" Height="36" Margin="0,0,8,8" Content="VACUUM" Command="{Binding VacuumCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                        <Button MinWidth="120" Height="36" Margin="0,0,8,8" Content="Open Folder" Command="{Binding OpenFolderCommand}" Style="{StaticResource OutlineButtonStyle}"/>
                    </WrapPanel>

                    <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Margin="0,4,0,0" Foreground="DimGray" Text="Output / risultati:"/>
                    <TextBox Grid.Row="5" Grid.ColumnSpan="2" Margin="0,6,0,0" MinHeight="120"
                             Text="{Binding OutputLog}"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             Style="{StaticResource ModernTextBoxStyle}"/>
                </Grid>
            </ScrollViewer>
        </Border>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button MinWidth="110" Height="42" Content="Chiudi" IsCancel="True" Style="{StaticResource OutlineButtonStyle}" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</chrome:DialogShellWindow>
