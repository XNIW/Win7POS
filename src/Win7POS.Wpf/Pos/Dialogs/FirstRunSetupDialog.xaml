<Window x:Class="Win7POS.Wpf.Pos.Dialogs.FirstRunSetupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurazione iniziale"
        Width="460"
        Height="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Border Padding="24" Background="{StaticResource BackgroundLightBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Configurazione iniziale" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,4"/>
            <TextBlock Grid.Row="1" Text="Crea il primo amministratore del sistema" FontSize="13" Foreground="#555" Margin="0,0,0,20"/>

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*" MinWidth="200"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,8" Text="Nome operatore:"/>
                <TextBox x:Name="DisplayNameBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" Style="{StaticResource ModernTextBoxStyle}" MinHeight="34"/>

                <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,8" Text="Username:"/>
                <TextBox x:Name="UsernameBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" Style="{StaticResource ModernTextBoxStyle}" MinHeight="34"/>

                <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,8" Text="PIN (4-6 cifre):"/>
                <PasswordBox x:Name="PinBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" MinHeight="34" Padding="8,6"/>

                <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,8" Text="Conferma PIN:"/>
                <PasswordBox x:Name="ConfirmPinBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8" MinHeight="34" Padding="8,6"/>
            </Grid>

            <TextBlock x:Name="ErrorText" Grid.Row="3" Foreground="#C62828" Margin="0,8,0,0" TextWrapping="Wrap" Visibility="Collapsed" VerticalAlignment="Top"/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Crea amministratore" MinWidth="120" Height="36" Margin="0,0,10,0" Click="OnCreateAdminClick" Style="{StaticResource PrimaryButtonStyle}" IsDefault="True"/>
                <Button Content="Annulla" MinWidth="90" Height="36" IsCancel="True" Style="{StaticResource OutlineButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
