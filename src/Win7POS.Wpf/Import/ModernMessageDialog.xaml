<Window x:Class="Win7POS.Wpf.Import.ModernMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Win7POS"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        MinWidth="360"
        MinHeight="140"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundLightBrush}">
    <Border Margin="16" Padding="20" Background="{StaticResource CardBgBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="TitleText" Grid.Row="0" FontWeight="SemiBold" FontSize="15" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,12"/>
            <TextBlock x:Name="MessageText" Grid.Row="1" TextWrapping="Wrap" FontSize="13" Foreground="#333" Margin="0,0,0,20"/>
            <Button Grid.Row="2" Content="OK" MinWidth="100" Height="36" HorizontalAlignment="Right" IsDefault="True" IsCancel="True"
                    Style="{StaticResource PrimaryButtonStyle}" Click="Ok_Click"/>
        </Grid>
    </Border>
</Window>
