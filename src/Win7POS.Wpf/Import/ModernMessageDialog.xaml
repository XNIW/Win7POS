<chrome:DialogShellWindow x:Class="Win7POS.Wpf.Import.ModernMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:Win7POS.Wpf.Chrome"
        Title="Win7POS"
        ShowHeader="False"
        UseModalOverlay="True"
        DialogCornerRadius="16"
        DialogBackground="#F1EDF7"
        DialogBorderBrush="#D9CEE8"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        MinWidth="360"
        MinHeight="140"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="24" Background="#F1EDF7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="18" FontWeight="SemiBold" Foreground="#5E3F86" Margin="0,0,0,16"/>
        <Border Grid.Row="1" Background="#FCFBFE" BorderBrush="#E4DBEF" BorderThickness="1" CornerRadius="10" Padding="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="MessageText" Grid.Row="0" TextWrapping="Wrap" FontSize="13" Foreground="#333" Margin="0,0,0,20"/>
            <Button Grid.Row="1" Content="OK" MinWidth="100" Height="42" HorizontalAlignment="Right" IsDefault="True" IsCancel="True"
                    Style="{StaticResource PrimaryButtonStyle}" Click="Ok_Click"/>
        </Grid>
        </Border>
    </Grid>
</chrome:DialogShellWindow>
