<UserControl x:Class="Win7POS.Wpf.Products.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1100">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="0,0,0,12" Background="{StaticResource BackgroundLightBrush}" Padding="14" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" MinHeight="36" Style="{StaticResource ModernTextBoxStyle}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <Button Grid.Column="1" Margin="10,0,0,0" MinWidth="100" MinHeight="36" Content="Cerca" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SearchCommand}"/>
                <Button Grid.Column="2" Margin="10,0,0,0" MinWidth="130" MinHeight="36" Content="Salva selezionato" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding SaveSelectedCommand}"/>
                <Button Grid.Column="3" Margin="10,0,0,0" MinWidth="120" MinHeight="36" Content="Export CSV" Style="{StaticResource OutlineButtonStyle}" Command="{Binding ExportCsvCommand}"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="0">
        <DataGrid
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" IsReadOnly="True" Width="120"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
                <DataGridTextColumn Header="Vendita" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                <DataGridTextColumn Header="Cod. articolo" Binding="{Binding ArticleCode}" IsReadOnly="True" Width="100"/>
                <DataGridTextColumn Header="Nome2" Binding="{Binding Name2}" IsReadOnly="True" Width="1*"/>
                <DataGridTextColumn Header="Acquisto" Binding="{Binding PurchasePrice}" IsReadOnly="True" Width="80"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding StockQty}" IsReadOnly="True" Width="60"/>
                <DataGridTextColumn Header="Fornitore" Binding="{Binding SupplierName}" IsReadOnly="True" Width="1*"/>
                <DataGridTextColumn Header="Categoria" Binding="{Binding CategoryName}" IsReadOnly="True" Width="1*"/>
            </DataGrid.Columns>
        </DataGrid>
        </Border>

        <TextBlock Grid.Row="2" Margin="0,12,0,0" Foreground="#6B5B73" Text="{Binding StatusMessage}"/>
    </Grid>
</UserControl>
