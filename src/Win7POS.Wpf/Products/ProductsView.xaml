<UserControl x:Class="Win7POS.Wpf.Products.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1100">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" MinHeight="30" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            <Button Grid.Column="1" Margin="8,0,0,0" MinWidth="90" Content="Cerca" Command="{Binding SearchCommand}"/>
            <Button Grid.Column="2" Margin="8,0,0,0" MinWidth="120" Content="Salva selezionato" Command="{Binding SaveSelectedCommand}"/>
            <Button Grid.Column="3" Margin="8,0,0,0" MinWidth="110" Content="Export CSV" Command="{Binding ExportCsvCommand}"/>
        </Grid>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" IsReadOnly="True" Width="120"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
                <DataGridTextColumn Header="Vendita" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                <DataGridTextColumn Header="Cod. articolo" Binding="{Binding ArticleCode}" IsReadOnly="True" Width="100"/>
                <DataGridTextColumn Header="Nome2" Binding="{Binding Name2}" IsReadOnly="True" Width="1*"/>
                <DataGridTextColumn Header="Acquisto" Binding="{Binding PurchasePrice}" IsReadOnly="True" Width="80"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding StockQty}" IsReadOnly="True" Width="60"/>
                <DataGridTextColumn Header="Fornitore" Binding="{Binding SupplierName}" IsReadOnly="True" Width="1*"/>
                <DataGridTextColumn Header="Categoria" Binding="{Binding CategoryName}" IsReadOnly="True" Width="1*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Margin="0,8,0,0" Foreground="DimGray" Text="{Binding StatusMessage}"/>
    </Grid>
</UserControl>
