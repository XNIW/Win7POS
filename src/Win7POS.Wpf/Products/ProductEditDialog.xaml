<Window x:Class="Win7POS.Wpf.Products.ProductEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}"
        Width="520"
        Height="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Barcode -->
        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Barcode:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="8,0,0,0"
                 Text="{Binding Barcode, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource ModernTextBoxStyle}"
                 IsReadOnly="{Binding IsBarcodeReadOnly}"/>

        <!-- Nome -->
        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,10,0,0" VerticalAlignment="Center" Text="Nome:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="8,10,0,0"
                 Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource ModernTextBoxStyle}"/>

        <!-- Prezzo vendita -->
        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,10,0,0" VerticalAlignment="Center" Text="Prezzo vendita (CLP):"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="8,10,0,0"
                 Text="{Binding PriceText, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource ModernTextBoxStyle}"/>

        <!-- Prezzo acquisto -->
        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,10,0,0" VerticalAlignment="Center" Text="Prezzo acquisto:"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="8,10,0,0"
                 Text="{Binding PurchasePriceText, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource ModernTextBoxStyle}"/>

        <!-- Fornitore -->
        <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,10,0,0" VerticalAlignment="Center" Text="Fornitore:"/>
        <ComboBox Grid.Row="4" Grid.Column="1" Margin="8,10,0,0" MinHeight="34"
                  Style="{StaticResource ModernComboBoxStyle}"
                  ItemsSource="{Binding Suppliers}"
                  DisplayMemberPath="Name"
                  SelectedItem="{Binding SelectedSupplier}"/>

        <!-- Categoria -->
        <TextBlock Grid.Row="5" Grid.Column="0" Margin="0,10,0,0" VerticalAlignment="Center" Text="Categoria:"/>
        <ComboBox Grid.Row="5" Grid.Column="1" Margin="8,10,0,0" MinHeight="34"
                  Style="{StaticResource ModernComboBoxStyle}"
                  ItemsSource="{Binding Categories}"
                  DisplayMemberPath="Name"
                  SelectedItem="{Binding SelectedCategory}"/>

        <!-- Stock: singola riga numerica compatta -->
        <TextBlock Grid.Row="6" Grid.Column="0" Margin="0,10,0,0" VerticalAlignment="Center" Text="{Binding StockLabel, Mode=OneWay}"/>
        <TextBox Grid.Row="6" Grid.Column="1" Margin="8,10,0,0" MinHeight="32" MaxHeight="32"
                 VerticalAlignment="Center"
                 Text="{Binding StockText, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource ModernTextBoxStyle}"/>

        <!-- Cod. articolo / Nome2 (opzionale) -->
        <TextBlock Grid.Row="7" Grid.Column="0" Margin="0,10,0,0" VerticalAlignment="Center" Text="Cod. articolo / Nome2:"/>
        <Grid Grid.Row="7" Grid.Column="1" Margin="8,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding ArticleCode, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" MinHeight="32" MaxHeight="34" VerticalContentAlignment="Center"/>
            <TextBox Grid.Column="2" Text="{Binding Name2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" MinHeight="32" MaxHeight="34" VerticalContentAlignment="Center"/>
        </Grid>

        <!-- Pulsanti -->
        <StackPanel Grid.Row="9" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Annulla" MinWidth="100" Height="36" Margin="0,0,10,0" IsCancel="True"
                    Style="{StaticResource OutlineButtonStyle}" Command="{Binding CancelCommand}"/>
            <Button Content="Salva" MinWidth="100" Height="36" IsDefault="True"
                    Style="{StaticResource PrimaryButtonStyle}" Command="{Binding ConfirmCommand}"/>
        </StackPanel>
    </Grid>
</Window>
