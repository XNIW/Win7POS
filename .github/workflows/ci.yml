name: CI

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-check:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 8.0.x

      - name: Print .NET info
        run: dotnet --info

      - name: Restore CLI
        run: dotnet restore src/Win7POS.Cli/Win7POS.Cli.csproj

      - name: Restore WPF
        run: dotnet restore src/Win7POS.Wpf/Win7POS.Wpf.csproj

      - name: Dialog standards check
        shell: pwsh
        run: pwsh -File scripts/check-dialog-standards.ps1

      - name: Build Core (Release)
        run: dotnet build src/Win7POS.Core/Win7POS.Core.csproj -c Release --no-restore

      - name: Build CLI (Release)
        run: dotnet build src/Win7POS.Cli/Win7POS.Cli.csproj -c Release --no-restore

      - name: Selftest (fast)
        run: dotnet run --project src/Win7POS.Cli/Win7POS.Cli.csproj -c Release --no-build -- --selftest --keepdb

      - name: Build WPF (Release x86)
        run: dotnet build src/Win7POS.Wpf/Win7POS.Wpf.csproj -c Release -p:Platform=x86 -p:PlatformTarget=x86 --no-restore
